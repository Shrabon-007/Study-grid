# For the VPS Nginx setup, frontend and API share one domain.
VITE_API_BASE_URL=/api

# For a separately deployed frontend, use the full backend URL instead:
# VITE_API_BASE_URL=https://api.example.com/api
